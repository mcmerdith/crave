{
  "name": "crave-monorepo",
  "private": true,
  "scripts": {
    "app": "npm run start -w crave-app",
    "api": "npm run dev -w crave-api"
  },
  "workspaces": [
    "crave-api",
    "crave-app"
  ],
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
