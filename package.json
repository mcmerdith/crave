{
  "name": "crave-monorepo",
  "private": true,
  "scripts": {
    "app": "npm run start -w crave-app",
    "api": "npm run dev -w crave-api",
    "postinstall": "npm run postinstall -w crave-app"
  },
  "workspaces": [
    "crave-api",
    "crave-app"
  ],
  "dependencies": {
    "@react-native-community/slider": "5.0.1",
    "@react-spring/native": "^9.5.5",
    "expo-clipboard": "^8.0.7",
    "expo-linear-gradient": "^15.0.7",
    "prettier": "^3.6.2"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^5.0.2"
  }
}
